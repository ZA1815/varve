[package]
name = "varve-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "varve"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
varve = { version = "0.1.0", path = "../core" }
